# Пример без break, тк это плохой тон.
n = int(input()) # вводим значение переменной n
# устанавливаем начальное начение переменной фалага
flag = True 
i = 2
# На вход в цикл принимется 
# изначальное значение переменной flag
# Те в насале цикла запись flag, 
# это то же, что и flag == true
while flag:
    # если остаток при делении числа n на i равен 0
    if n % i == 0: 
        flag = False
        print(i)
     # делитель числа не может превышать 
     # введенное число, деленное на 2
    elif i > n // 2:
        print(n)
        flag = False
    i += 1

    # Вывод показывает в какой момент цикл прервался, 
    # из-за того, что значение flag сменилось на false
    print(flag) 